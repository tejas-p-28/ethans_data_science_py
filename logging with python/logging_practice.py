import logging

logging.basicConfig(level=logging.DEBUG,
                    format={}
                    )

logging.info('this is info msg')
logging.warning('this is warning msg')
logging.error('this is error msg')
logging.critical('this is the basic format')

logging.